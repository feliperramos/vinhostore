export type Product = {
  id: number;
  title: string;
  price: number;
  image: string;
  description: string;
  category: string;
};

export type Favorite = {
  id: number;
  title: string;
  price: number;
  image: string;
  createdAt: number;
};
